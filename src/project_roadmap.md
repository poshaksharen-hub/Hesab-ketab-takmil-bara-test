# 🗺️ نقشه راه و چک‌لیست پروژه (تکمیل شده)

این سند، نقشه راه اصلی ما برای توسعه و تکمیل ویژگی‌های پروژه **بود**. تمام فازهای تعریف شده در این نقشه راه با موفقیت به اتمام رسیده‌اند.

---

### ✅ فاز ۱: راه‌اندازی زیرساخت CI/CD برای پایگاه داده

**هدف:** خودکارسازی فرآیند به‌روزرسانی اسکیمای پایگاه داده (Migrations).

- [x] ایجاد پوشه `.github/workflows`.
- [x] ایجاد فایل `main.yml` به عنوان گردش کار اصلی و حذف فایل‌های اضافی.
- [x] تنظیم Secrets در GitHub (توسط کاربر انجام شد): `SUPABASE_DB_PASSWORD` و `SUPABASE_PROJECT_REF`.
- [x] تست نهایی و موفقیت‌آمیز workflow با یک `push` روی `main`.

---

### ✅ فاز ۲: پیاده‌سازی کامل مدل داده و منطق بک‌اند

**هدف:** ایجاد یک پایگاه داده پایدار و اتمی.

- [x] طراحی و پیاده‌سازی کامل اسکیمای پایگاه داده در `migration_roadmap.md`.
- [x] ایجاد توابع اتمی PostgreSQL برای تمام عملیات‌های حساس (پرداخت وام، پاس کردن چک، تحقق هدف و ...).
- [x] یکپارچه‌سازی منطق برنامه برای استفاده کامل از توابع RPC به جای محاسبات در کلاینت.

---

### ✅ فاز ۳: پیاده‌سازی قابلیت "پیوست فایل" (Attachment)

**هدف:** افزودن قابلیت آپلود، نمایش و حذف فایل برای موجودیت‌های مختلف.

- [x] **بخش هزینه‌ها (Expenses):**
  - [x] ایجاد تابع `uploadReceipt` در `src/lib/storage.ts`.
  - [x] به‌روزرسانی `expense-form.tsx` برای افزودن فیلد آپلود.
  - [x] به‌روزرسانی `transactions/page.tsx` برای ذخیره و حذف `attachment_path`.
- [x] **تعمیم به سایر بخش‌ها:**
  - [x] افزودن ستون `attachment_path` یا `image_path` به جداول `income`, `checks`, `loans`, `debts`, `goals`.
  - [x] اصلاح فرم‌ها و منطق ثبت/حذف در تمام بخش‌های مربوطه.

---

### ✅ فاز ۴: نمایش پیوست‌ها در رابط کاربری

**هدف:** نمایش فایل‌های آپلود شده به کاربر.

- [x] **بخش هزینه‌ها (Expenses):**
  - [x] به‌روزرسانی کامپوننت `ExpenseList` برای نمایش آیکون "مشاهده رسید".
- [x] **بخش اهداف مالی (Goals):**
  - [x] نمایش عکس آپلود شده در کارت مربوط به هر هدف.
- [x] تعمیم نمایش به تمام بخش‌های دیگر (`income`, `checks`, `loans`, `debts`).

---

### ✅ فاز ۵: تکمیل چرخه عمر موجودیت‌ها

**هدف:** مدیریت کامل فایل‌ها و داده‌ها (حذف هوشمند).

- [x] پیاده‌سازی منطق حذف فایل از Storage هنگام حذف موجودیت والد در تمام بخش‌ها.
- [x] پیاده‌سازی چرخه کامل عمر اهداف مالی (ایجاد، مشارکت، تحقق، بازگردانی، حذف).

---

### ✅ فاز ۶: تضمین کیفیت و نهایی‌سازی

**هدف:** اطمینان از پایداری و کیفیت نهایی محصول.
- [x] بازبینی و اصلاح تمام تست‌های سرتاسری Cypress برای مطابقت با آخرین تغییرات.
- [x] حذف تمام فایل‌های تست، مستندات و کدهای منسوخ و اضافی.
- [x] به‌روزرسانی فایل `README.md` برای منعکس کردن معماری نهایی پروژه.

---

**این پروژه با موفقیت به پایان رسید.**
