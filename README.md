
# حساب کتاب (Hesab Ketab)

**حساب کتاب** یک اپلیکیشن مدرن و جامع برای مدیریت مالی شخصی و خانوادگی است که با استفاده از جدیدترین تکنولوژی‌های وب ساخته شده است. این پروژه به کاربران (علی و فاطمه) اجازه می‌دهد تا درآمدها، هزینه‌ها، حساب‌های بانکی، چک‌ها، وام‌ها و اهداف مالی خود را به صورت یکپارچه و هوشمند مدیریت کنند.

---

## ✨ ویژگی‌های کلیدی

- **داشبورد جامع:** نمایش خلاصه‌ای از وضعیت مالی، دارایی خالص، درآمد و هزینه در بازه‌های زمانی مختلف.
- **مدیریت تراکنش‌ها:** ثبت آسان درآمدها و هزینه‌ها با جزئیات کامل.
- **کارت‌های بانکی:** مدیریت تمام حساب‌های بانکی شخصی و مشترک.
- **مدیریت تعهدات:** ثبت و پیگیری چک‌ها، وام‌ها و بدهی‌ها.
- **اهداف مالی:** تعریف اهداف مالی (مانند خرید یا سفر) و پس‌انداز برای رسیدن به آن‌ها.
- **تحلیل هوشمند:** استفاده از هوش مصنوعی برای ارائه تحلیل و پیشنهادهای مالی شخصی‌سازی شده.
- **رابط کاربری مدرن:** طراحی زیبا، واکنش‌گرا و کاملاً فارسی.

---

## 🏗️ معماری جریان داده: منطق "ثبت توسط" چگونه کار می‌کند؟

یکی از ویژگی‌های کلیدی این اپلیکیشن، قابلیت شناسایی کاربری است که هر تراکنش را ثبت کرده است. این منطق بر اساس یک جریان داده سه مرحله‌ای کار می‌کند:

1.  **ذخیره‌سازی شناسه کاربر (UID):**
    *   در لحظه ثبت هر تراکنش جدید (مانند وام، هزینه یا درآمد)، شناسه منحصر به فرد کاربر لاگین کرده (`uid`) از سیستم احراز هویت Supabase دریافت شده و در فیلدی به نام `registeredByUserId` در جدول مربوطه در پایگاه داده PostgreSQL ذخیره می‌شود.

2.  **فراخوانی لیست کامل کاربران:**
    *   برای جلوگیری از درخواست‌های مکرر، یک هوک مرکزی به نام `useDashboardData` لیست کامل تمام کاربران (شامل علی و فاطمه) را از پایگاه داده فراخوانی می‌کند. این باعث می‌شود که اطلاعات پروفایل هر دو کاربر همیشه در دسترس برنامه باشد.

3.  **نمایش نام کاربر:**
    *   کامپوننت‌هایی که وظیفه نمایش لیست‌ها را دارند (مانند `LoanList` یا `ExpenseList`)، برای هر تراکنش، مقدار فیلد `registeredByUserId` را با لیست کامل کاربران مقایسه کرده و پس از یافتن کاربر متناظر، نام کوچک (`firstName`) او را در رابط کاربری نمایش می‌دهند.

---

## 🛠️ تکنولوژی‌های استفاده شده

- **فریمورک:** [Next.js](https://nextjs.org/) (App Router)
- **زبان:** [TypeScript](https://www.typescriptlang.org/)
- **رابط کاربری (UI):** [React](https://react.dev/), [ShadCN UI](https://ui.shadcn.com/), [Tailwind CSS](https://tailwindcss.com/)
- **پایگاه داده و احراز هویت:** [Supabase](https://supabase.io/) (PostgreSQL, Auth, Storage)
- **هوش مصنوعی:** [Genkit](https://firebase.google.com/docs/genkit)
- **استقرار (Deployment):** [Netlify](https://www.netlify.com/)

---

## 🚀 شروع به کار

این پروژه برای اجرا روی پلتفرم Netlify پیکربندی شده است.

### پیش‌نیازها
- [Node.js](https://nodejs.org/) (نسخه 18 یا بالاتر)
- [Supabase CLI](https://supabase.com/docs/guides/cli)
- [Docker](https://www.docker.com/products/docker-desktop/) (برای اجرای Supabase به صورت محلی)

### مراحل نصب و اجرا

1.  **کلون کردن ریپازیتوری:**
    ```bash
    git clone https://github.com/poshaksharen-hub/studio.git
    cd studio
    ```

2.  **نصب وابستگی‌ها:**
    ```bash
    npm install
    ```

3. **راه اندازی Supabase محلی**
    ```bash
    supabase start
    ```

4.  **اجرای پروژه در حالت توسعه:**
    ```bash
    npm run dev
    ```

5.  **ایجاد بیلد برای محیط پروداکشن:**
    ```bash
    npm run build
    ```
