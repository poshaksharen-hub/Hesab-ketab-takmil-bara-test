م # حساب کتاب (Hesab Ketab)

**حساب کتاب** یک اپلیکیشن مدرن و جامع برای مدیریت مالی شخصی و خانوادگی است که با استفاده از جدیدترین تکنولوژی‌های وب ساخته شده است. این پروژه به کاربران (علی و فاطمه) اجازه می‌دهد تا درآمدها، هزینه‌ها، حساب‌های بانکی، چک‌ها، وام‌ها و اهداف مالی خود را به صورت یکپارچه و هوشمند مدیریت کنند.

---

## ✨ ویژگی‌های کلیدی

- **داشبورد جامع:** نمایش خلاصه‌ای از وضعیت مالی، دارایی خالص، درآمد و هزینه در بازه‌های زمانی مختلف.
- **مدیریت تراکنش‌ها:** ثبت آسان درآمدها و هزینه‌ها با جزئیات کامل و قابلیت پیوست رسید.
- **کارت‌های بانکی:** مدیریت تمام حساب‌های بانکی شخصی و مشترک با طراحی مدرن.
- **مدیریت تعهدات:** ثبت و پیگیری چک‌ها، وام‌ها و بدهی‌ها به صورت اتمی و امن.
- **اهداف مالی:** تعریف اهداف مالی (مانند خرید یا سفر) با قابلیت آپلود عکس و پس‌انداز تدریجی.
- **گفتگوی زنده:** یکپارچه‌سازی اعلان تراکنش‌ها در یک محیط چت زنده برای هر دو کاربر.
- **پایگاه داده قدرتمند:** استفاده از توابع PostgreSQL برای تضمین یکپارچگی داده‌ها در عملیات پیچیده.
- **استقرار خودکار:** پیاده‌سازی CI/CD برای به‌روزرسانی خودکار اسکیمای پایگاه داده با هر تغییر.

---

## 🏗️ معماری نهایی پروژه

این پروژه با یک معماری مدرن و چندلایه طراحی شده است تا از پایداری، امنیت و توسعه‌پذیری اطمینان حاصل شود.

### ۱. رابط کاربری (Frontend)
- **Next.js (App Router) & React:** برای ساخت یک رابط کاربری سریع، واکنش‌گرا و بهینه‌شده برای سئو.
- **ShadCN UI & Tailwind CSS:** برای ایجاد یک سیستم طراحی زیبا، سازگار و کاملاً قابل تنظیم.
- **TypeScript:** برای تضمین کیفیت و کاهش خطاها از طریق نوع‌دهی ایستا (Static Typing).

### ۲. مدیریت وضعیت متمرکز (State Management)
- **React Context API (`useDashboardData`):** به جای پراکنده‌سازی منطق واکشی داده، یک Context متمرکز تمام داده‌های برنامه (هزینه‌ها، درآمدها، حساب‌ها و ...) را مدیریت می‌کند.
- **واکنش‌گرایی آنی:** این Context یک تابع `refreshData` را در اختیار تمام کامپوننت‌ها قرار می‌دهد. پس از هر عملیات ثبت، ویرایش یا حذف، این تابع فراخوانی شده و کل برنامه به صورت خودکار و بدون نیاز به رفرش صفحه، با آخرین داده‌ها به‌روز می‌شود.
- **Real-time Updates:** با استفاده از Supabase Realtime, هرگونه تغییر در پایگاه داده (حتی از طریق دستگاه دیگر) به صورت خودکار شناسایی شده و باعث فراخوانی `refreshData` می‌شود، که تجربه کاربری کاملاً زنده‌ای را فراهم می‌کند.

### ۳. پایگاه داده و بک‌اند (Backend)
- **Supabase (PostgreSQL):** به عنوان یک بک‌اند کامل (Backend-as-a-Service) شامل پایگاه داده، احراز هویت، و فضای ذخیره‌سازی فایل.
- **توابع اتمی PostgreSQL:** قلب تپنده منطق کسب‌وکار ما. عملیات‌های پیچیده و حساس مانند "پاس کردن چک"، "پرداخت قسط وام" یا "افزودن به پس‌انداز هدف" به جای اجرا در کلاینت، در قالب توابع SQL در پایگاه داده پیاده‌سازی شده‌اند. این رویکرد تضمین می‌کند که تمام مراحل یک عملیات (مانند کسر از موجودی، ثبت هزینه و آپدیت رکورد) به صورت **اتمی** (در یک تراکنش واحد) انجام شوند و از ناهماهنگی داده‌ها جلوگیری می‌کند.
- **RLS (Row Level Security):** برای اطمینان از اینکه کاربران فقط به داده‌های مجاز خود دسترسی دارند.

### ۴. استقرار و CI/CD
- **GitHub Actions:** یک گردش کار (Workflow) خودکار تنظیم شده است که به فایل `migration_roadmap.md` حساس است.
- **مدیریت آسان Schema:** برای هرگونه تغییر در ساختار پایگاه داده، کافیست دستورات SQL جدید به انتهای فایل `migration_roadmap.md` اضافه شوند. پس از `push` کردن این تغییر، GitHub Action به صورت خودکار به Supabase متصل شده و اسکریپت را اجرا می‌کند تا پایگاه داده به‌روز شود.

---

## 🚀 شروع به کار

این پروژه برای اجرا روی پلتفرم Netlify پیکربندی شده است.

### پیش‌نیازها
- [Node.js](https://nodejs.org/) (نسخه 18 یا بالاتر)
- [Supabase CLI](https://supabase.com/docs/guides/cli)
- [Docker](https://www.docker.com/products/docker-desktop/) (برای اجرای Supabase به صورت محلی)

### مراحل نصب و اجرا

1.  **کلون کردن ریپازیتوری:**
    ```bash
    git clone https://github.com/poshaksharen-hub/studio.git
    cd studio
    ```

2.  **نصب وابستگی‌ها:**
    ```bash
    npm install
    ```

3. **راه اندازی Supabase محلی**
    ```bash
    supabase start
    ```

4.  **اجرای پروژه در حالت توسعه:**
    ```bash
    npm run dev
    ```

5.  **اجرای تست‌های سرتاسری (Cypress):**
    ```bash
    npm run cypress:run
    ```
