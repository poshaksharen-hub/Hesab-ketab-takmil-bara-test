# 🗺️ نقشه راه و چک‌لیست پروژه

این سند، نقشه راه اصلی ما برای توسعه و تکمیل ویژگی‌های پروژه است.

---

### فاز ۱: راه‌اندازی زیرساخت CI/CD برای پایگاه داده

**هدف:** خودکارسازی فرآیند به‌روزرسانی اسکیمای پایگاه داده (Migrations).

- [x] ساخت پوشه `.github/workflows`.
- [x] ساخت فایل `supabase_migration.yml`.
- [ ] **(در انتظار شما)** تنظیم Secrets در GitHub:
  - [ ] `PROJECT_ID`
  - [ ] `SUPABASE_ACCESS_TOKEN`
  - [ ] `SUPABASE_DB_PASSWORD`
- [ ] تست نهایی workflow با یک `push` روی `main`.

---

### فاز ۲: پیاده‌سازی قابلیت "پیوست فایل" (Attachment)

**هدف:** افزودن قابلیت آپلود و ذخیره فایل (رسید، عکس، سند) برای موجودیت‌های مختلف.

- [x] **بخش هزینه‌ها (Expenses):**
  - [x] ایجاد فایل migration برای افزودن ستون `receipt_path` (به‌صورت دستی در SQL Editor اجرا شد).
  - [x] ایجاد تابع `uploadReceipt` در `src/lib/storage.ts`.
  - [x] به‌روزرسانی `expense-form.tsx` برای افزودن فیلد آپلود.
  - [x] به‌روزرسانی `transactions/page.tsx` برای ذخیره `receipt_path` در دیتابیس.
- [ ] **تعمیم به سایر بخش‌ها:**
  - [x] ایجاد فایل migration جامع برای افزودن ستون `attachment_path` یا `image_path` به جداول `income`, `checks`, `loans`, `debts`, `goals` (به‌صورت دستی در SQL Editor اجرا شد).
  - [ ] **بخش اهداف مالی (Goals):**
    - [ ] اصلاح فرم `goal-form` برای آپلود عکس.
    - [ ] اصلاح صفحه `goals/page` برای ذخیره آدرس عکس.
  - [ ] تعمیم به بخش‌های `income`, `checks`, `loans`, `debts`.

---

### فاز ۳: نمایش پیوست‌ها در رابط کاربری

**هدف:** نمایش فایل‌های آپلود شده به کاربر.

- [ ] **بخش هزینه‌ها (Expenses):**
  - [ ] به‌روزرسانی کامپوننت `ExpenseList` برای نمایش آیکون "مشاهده رسید" و باز کردن آن در یک تب جدید.
- [ ] **بخش اهداف مالی (Goals):**
  - [ ] نمایش عکس آپلود شده در کارت مربوط به هر هدف.
- [ ] تعمیم نمایش به سایر بخش‌ها.

---

### فاز ۴: تکمیل چرخه عمر پیوست‌ها

**هدف:** مدیریت کامل فایل‌ها (حذف هوشمند).

- [ ] پیاده‌سازی منطق حذف فایل از Storage هنگام حذف موجودیت والد (مثلاً حذف عکس رسید هنگام حذف یک هزینه).
