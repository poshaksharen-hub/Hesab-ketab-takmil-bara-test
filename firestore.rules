rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow any authenticated user to perform any action on any document.
    // This is insecure for production but is used here to unblock development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
