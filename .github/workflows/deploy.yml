<<<<<<< HEAD
name: Deploy with Connection Pooler (Final)
=======
name: Deploy Supabase Schema via Web
>>>>>>> d6e64e62c84ca60396d5dbf5edd41101e8a97687

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

<<<<<<< HEAD
      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Apply SQL Schema via Connection Pooler
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SUPABASE_POOL_HOST: ${{ secrets.SUPABASE_POOL_HOST }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          echo "Connecting to the Supabase connection pooler at ${{ secrets.SUPABASE_POOL_HOST }}..."
          
          SQL_SCRIPT=$(cat supabase/migrations/20240720000000_initial_schema.sql)

          # Using the correct username format and port for the Session Pooler
          POOLER_USERNAME="postgres.${{ secrets.SUPABASE_PROJECT_REF }}"
          
          PGPASSWORD="$SUPABASE_DB_PASSWORD" psql \
            --host="${{ secrets.SUPABASE_POOL_HOST }}" \
            --port=5432 \
            --username="$POOLER_USERNAME" \
            --dbname=postgres \
            -a \
            -c "$SQL_SCRIPT"

          echo "Schema applied successfully via Connection Pooler!"
=======
      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y postgresql-client dnsutils

      - name: Apply SQL Schema with Aggressive DNS Resolution
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          set -x 
          
          DB_HOST="db.${{ secrets.SUPABASE_PROJECT_REF }}.supabase.co"
          DB_IP=""

          echo "--- STARTING DNS RESOLUTION ---"
          
          echo "Strategy 1: Standard 'dig'"
          DB_IP=$(dig +short A $DB_HOST)
          
          if [ -z "$DB_IP" ]; then
            echo "Strategy 1 FAILED. Trying Strategy 2: 'dig' via Google DNS"
            DB_IP=$(dig +short A @8.8.8.8 $DB_HOST)
          fi
          
          if [ -z "$DB_IP" ]; then
            echo "Strategy 2 FAILED. Trying Strategy 3: 'nslookup'"
            DB_IP=$(nslookup $DB_HOST | awk '/^Address: / { print $2 }' | tail -n 1)
          fi

          if [ -z "$DB_IP" ]; then
            echo "::error::All DNS strategies failed. Cannot resolve $DB_HOST."
            exit 1
          fi
          
          echo "SUCCESS: Resolved $DB_HOST to IPv4 Address: $DB_IP"
          
          SQL_SCRIPT=$(cat supabase/migrations/20240720000000_initial_schema.sql)
          
          PGPASSWORD="$SUPABASE_DB_PASSWORD" psql \
            --host="$DB_IP" \
            --port=5432 \
            --username=postgres \
            --dbname=postgres \
            -a \
            -c "$SQL_SCRIPT"
            
>>>>>>> d6e64e62c84ca60396d5dbf5edd41101e8a97687
